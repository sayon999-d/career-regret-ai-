apiVersion: apps/v1
kind: Deployment
metadata:
  name: stepwise-ai-app
  namespace: stepwise-ai
  labels:
    app: stepwise-ai
    component: app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stepwise-ai
      component: app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: stepwise-ai
        component: app
    spec:
      containers:
        - name: stepwise-ai
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: stepwise-ai-config
            - secretRef:
                name: stepwise-ai-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: app-data
              mountPath: /app/data
            - name: app-exports
              mountPath: /app/exports
            - name: app-logs
              mountPath: /app/logs
      volumes:
        - name: app-data
          persistentVolumeClaim:
            claimName: stepwise-ai-data-pvc
        - name: app-exports
          persistentVolumeClaim:
            claimName: stepwise-ai-exports-pvc
        - name: app-logs
          emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: stepwise-ai
  labels:
    app: stepwise-ai
    component: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stepwise-ai
      component: redis
  template:
    metadata:
      labels:
        app: stepwise-ai
        component: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          command: ["redis-server", "--appendonly", "yes"]
          ports:
            - containerPort: 6379
              protocol: TCP
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          persistentVolumeClaim:
            claimName: stepwise-ai-redis-pvc
